[database]
host = localhost
port = 3306
user = gemp
password = your_password_here
name = gemp_db

[paths]
# Base path where replay files are stored
# Summary files expected at: {replay_base}/summaries/YYYY/MM/*.json
replay_base = /replay

# Blueprint mapping file for promo/alt-art normalization
mapping_file = blueprintMapping.txt
